{
   "private": true,
   "scripts": {
      "build:server": "lerna run build --scope={@abb/common,@abb/server}"
   },
   "workspaces": [
      "packages/*"
   ],
   "devDependencies": {
      "lerna": "^2.11.0"
   }
}
